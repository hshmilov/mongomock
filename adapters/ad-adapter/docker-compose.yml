version: '3'
services:
  ad-adapter:
        build: .
        image: axonius/ad-adapter
        container_name: "ad-adapter"
        ports:
            - 5001:80
        volumes:
          - ./src:/home/axonius/app
          - ../../plugins/axonius-libs/src/libs:/home/axonius/libs
          - ../../logs:/home/axonius/logs

networks:
  default:
    external:
      name: axonius