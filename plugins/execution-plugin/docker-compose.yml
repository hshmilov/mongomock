version: '3'
services:
  execution:
        build: .
        image: axonius/execution
        container_name: "execution"
        ports:
          - 4999:80
        volumes:
          - ./src:/home/axonius/app
          - ../axonius-libs/src/libs:/home/axonius/libs
          - ../../logs:/home/axonius/logs

networks:
  default:
    external:
      name: axonius