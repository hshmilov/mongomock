version: '3'
services:
  mongodb:
        image: mongo
        container_name: "mongo"
        environment:
          - MONGO_INITDB_ROOT_USERNAME=ax_user
          - MONGO_INITDB_ROOT_PASSWORD=IAmDeanSysMan
          - MONGO_INITDB_DATABASE=demo
        volumes:
          - ./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
          - ./mongodump:/mongodump
        ports:
            - 27017:27017


networks:
  default:
    external:
      name: axonius