version: '3'
services:
  careful-execution-correlator-plugin:
        build: .
        image: axonius/careful-execution-correlator-plugin
        container_name: "careful-execution-correlator-plugin"
        ports:
          - 5039:80
        volumes:
          - ../../logs/careful-execution-correlator-plugin:/home/axonius/logs
          - data:/home/axonius

volumes:
  data:

networks:
  default:
    external:
      name: axonius